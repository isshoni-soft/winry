package tv.isshoni.winry.entity.bootstrap;

// TODO: Make this extendable instead of an enum, allowing people to register their own types w/ independent weights
public enum BootstrapClassType {
    BOOTSTRAP_CLASS,
    INJECTED_DEFAULT,
    INJECTED_DATABASE,
    INJECTED_SERVICE
}
